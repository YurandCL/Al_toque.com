<%= form_with model: @articulo, local: true do |form| %>

	<%if @articulo.errors.any? %>
		<div id="error_explanation">
			<h2>
				<%= pluralize(@articulo.errors.count, "error") %>, No se puede guardar.	
			</h2>
			<ul>
				<% @articulo.errors.full_messages.each do |msg| %>
					<li><%= msg %></li>
				<% end %>
			</ul>
		</div>
	<% end %>
	
	<p>
		<%= form.label :title, 'Título' %><br>
		<%= form.text_field :title %>
	</p>

	<p>
		<%= form.label :text, 'Contenido' %><br>
		<%= form.text_area :text %>
	</p>

	<p>
		<%= form.submit %>
	</p>

<% end %>
